/* wraps the entire app with the global context providers.
 A Provider in react makes a value like state,functions,config 
 available to  every component in the tree - without manually passing props down. 
 */

export {};
